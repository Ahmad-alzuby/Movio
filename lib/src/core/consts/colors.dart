import 'package:flutter/widgets.dart';

abstract class MyColor {
  static const primeryColor = Color.fromARGB(255, 35, 33, 33);
  static const textColor = Color.fromARGB(210, 180, 180, 180);
  static const formColor = Color.fromARGB(255, 90, 90, 90);
}
